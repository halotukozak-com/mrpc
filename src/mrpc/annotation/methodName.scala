package mrpc.annotation

import made.annotation.MetaAnnotation

/** Marker tagging a raw param as the injection site for the resolved method name. Mirrors commons `methodName` (marker, no param). */
final class methodName extends MetaAnnotation
